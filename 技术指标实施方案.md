# 技术指标实施方案

## 1. 背景与目标

当前 `stock-agent/trading-agent` 已具备以下基础能力：

- 使用 SQLite 存储 `market_bars_1d`
- 通过 `scripts/stock_agent.py` 统一暴露 CLI
- 已实现 `strategy-150MA:v1` 的信号、回测、交易计划、日报
- 已通过 `extensions/trading-plugin` 暴露给 OpenClaw

本次改造目标是：

- 将技术分析能力沉淀为独立的 runtime 模块
- 让技术分析能力与具体策略解耦
- 将 `150MA` 策略对 MA 的使用改为复用技术指标模块
- 为后续形态识别、趋势识别、波动识别和标准化特征输出打底

本次范围内先交付：

- 通用技术指标计算模块
- MA 指标接入 `150MA` 策略
- 可扩展的技术分析 CLI 和插件动作
- 面向后续能力的文件结构与接口约定

暂不在本轮内强制交付：

- 全量指标缓存落库
- 新技术分析策略上线
- 复杂形态库或机器学习识别

## 2. 设计原则

### 2.1 技术分析与策略解耦

职责边界如下：

- 技术指标模块负责“算指标、产出技术状态、提供特征”
- 策略模块负责“引用指标、生成买卖规则、回测和交易计划”
- skill 层继续只负责 workflow、解释、输出约束，不承担真实计算

### 2.2 保持现有运行时形态

保留当前主入口：

- `stock-agent/trading-agent/scripts/stock_agent.py`

新增模块后，`stock_agent.py` 只做：

- 参数解析
- 数据库连接
- 调度指标模块
- 调度策略模块
- 输出 JSON

### 2.3 先按需计算，不急于落库

当前数据规模仍以行业 ETF 日线为主，优先采用：

- SQLite 存原始 K 线
- pandas / TA-Lib 按需计算指标

只有在批量扫描性能不达标时，再考虑新增特征缓存表。

## 3. 目标文件结构

本次建议调整为：

```text
stock-agent/
  trading-agent/
    README.md
    requirements.txt
    AGENT_DEVELOPMENT.md
    EXTENSIBILITY.md
    references/
      schema.sql
      openclaw.trading-agent.example.json
    scripts/
      stock_agent.py
      indicators.py
      indicators_config.py
      technical_analysis.py
      strategies/
        __init__.py
        strategy_150ma.py
    strategies/
      strategy-150ma-v1/
        README.md
        params.json
      technical-analysis/
        README.md
```

说明：

- `indicators.py`：纯指标计算
- `indicators_config.py`：统一指标参数与窗口定义
- `technical_analysis.py`：技术分析聚合层，负责组织指标输出与后续扩展位
- `scripts/strategies/strategy_150ma.py`：从 `stock_agent.py` 中抽出 150MA 策略实现
- `strategies/technical-analysis/README.md`：记录技术分析模块能力、字段定义与扩展约束

## 4. 文件级实施清单

### 4.1 `stock-agent/trading-agent/requirements.txt`

#### 目标

补齐技术指标依赖。

#### 修改项

- 保留现有：
  - `akshare>=1.16.0`
  - `pandas>=2.2.0`
  - `pyecharts>=2.0.0`
- 新增：
  - `TA-Lib` 或可在 WSL Ubuntu 中安装的对应 Python 包名

#### 结果要求

- WSL Ubuntu 中可通过 `python3 -c "import talib"` 验证导入
- 文档中注明如果缺少系统级 TA-Lib 依赖时的安装方式

### 4.2 `stock-agent/trading-agent/scripts/indicators_config.py`

#### 目标

集中管理技术指标参数，避免策略或 CLI 分散写死窗口值。

#### 新增内容

- 默认均线窗口定义
  - `MA_WINDOWS = [5, 10, 20, 60, 150]`
  - `EMA_WINDOWS = [12, 26]`
- 默认指标参数定义
  - `MACD_FAST = 12`
  - `MACD_SLOW = 26`
  - `MACD_SIGNAL = 9`
  - `RSI_WINDOWS = [6, 14]`
  - `KDJ_WINDOW = 9`
  - `BOLL_WINDOW = 20`
  - `BOLL_STD = 2`
  - `ATR_WINDOW = 14`
  - `ADX_WINDOW = 14`

#### 结果要求

- 所有技术分析与策略代码统一从这里取参数
- 后续新增策略版本时可按版本覆写参数

### 4.3 `stock-agent/trading-agent/scripts/indicators.py`

#### 目标

建设通用技术指标模块。

#### 新增核心函数

- `load_talib()`
  - 负责导入 `talib`
  - 缺失时抛出明确错误
- `compute_ma(df, windows)`
- `compute_ema(df, windows)`
- `compute_macd(df, fast, slow, signal)`
- `compute_rsi(df, windows)`
- `compute_kdj(df, window)`
- `compute_boll(df, window, num_std)`
- `compute_atr(df, window)`
- `compute_obv(df)`
- `compute_adx(df, window)`
- `compute_indicators(df, indicator_set=None)`

#### 输出列规范

- MA：
  - `ma_5`, `ma_10`, `ma_20`, `ma_60`, `ma_150`
- EMA：
  - `ema_12`, `ema_26`
- MACD：
  - `macd_dif`, `macd_dea`, `macd_hist`
- RSI：
  - `rsi_6`, `rsi_14`
- KDJ：
  - `kdj_k`, `kdj_d`, `kdj_j`
- BOLL：
  - `boll_mid`, `boll_upper`, `boll_lower`, `boll_width`
- ATR：
  - `atr_14`
- OBV：
  - `obv`
- ADX：
  - `adx_14`

#### 实现要求

- 函数输入输出均为 pandas DataFrame
- 不直接依赖 SQLite
- 指标计算过程中不修改调用方原始 DataFrame，先复制再写列
- 对于窗口不足的数据保留 `NaN`，由上层统一处理
- `KDJ` 可使用 pandas 自实现，其他优先用 TA-Lib

### 4.4 `stock-agent/trading-agent/scripts/technical_analysis.py`

#### 目标

提供技术分析聚合层，统一面向 CLI、详情页、策略和未来识别模块。

#### 新增核心函数

- `build_technical_dataframe(df, indicator_set=None)`
  - 调用 `compute_indicators(...)`
- `build_latest_indicator_snapshot(df)`
  - 返回最新交易日的指标快照
- `build_recent_indicator_series(df, recent_bars)`
  - 返回最近 N 条 K 线对应的指标序列
- `build_technical_payload(df, recent_bars=20, include_series=False)`
  - 返回标准化技术分析输出

#### 输出结构建议

```json
{
  "kind": "technical_analysis",
  "trade_date": "2026-03-11",
  "indicators": {
    "ma_5": 1.23,
    "ma_150": 1.11,
    "macd_dif": 0.02,
    "rsi_14": 61.5
  },
  "series": []
}
```

#### 结果要求

- 供 `etf_detail` 直接引用
- 供 `150MA` 策略读取 `ma_150`
- 为后续形态识别、趋势识别、波动识别预留统一入口

### 4.5 `stock-agent/trading-agent/scripts/strategies/__init__.py`

#### 目标

建立策略模块目录，避免所有策略逻辑继续堆在 `stock_agent.py`。

#### 新增内容

- 导出 `strategy_150ma` 模块公共函数

### 4.6 `stock-agent/trading-agent/scripts/strategies/strategy_150ma.py`

#### 目标

将 `150MA` 策略实现改为依赖通用指标模块，而不是自行滚动计算。

#### 迁移函数

从 `scripts/stock_agent.py` 抽出并重构：

- `build_strategy_data_check(...)`
- `compute_signal_150ma(...)`
- `run_backtest_150ma(...)`
- `batch_signal_150ma(...)`
- `batch_backtest_150ma(...)`
- `create_trade_plan(...)`

#### 重构重点

1. `compute_signal_150ma(...)`
- 不再直接写：
  - `df["ma150"] = df["close"].rolling(150).mean()`
- 改为：
  - 调用 `build_technical_dataframe(...)`
  - 使用 `ma_150` 列

2. `run_backtest_150ma(...)`
- 不再自行构造 MA
- 改为复用指标模块后的 DataFrame
- 买卖逻辑仍保持：
  - 上穿 `ma_150` 买入
  - 下穿 `ma_150` 卖出

3. `build_strategy_data_check(...)`
- 当前仍可保留在策略层
- 但其 warmup 要与 `MA 150` 的指标预热逻辑保持一致

#### 结果要求

- 150MA 策略的行为结果与当前版本尽量一致
- 但 MA 来源统一由技术指标模块提供

### 4.7 `stock-agent/trading-agent/scripts/stock_agent.py`

#### 目标

把 CLI 主入口改为调度器，并增加技术分析命令。

#### 修改项一：导入与结构整理

- 引入：
  - `technical_analysis.py`
  - `strategies/strategy_150ma.py`
- 保留数据库、同步、查询、详情、公共工具函数
- 删除或迁移策略实现中的 MA 计算细节

#### 修改项二：新增 CLI 命令

新增子命令：

- `technical-indicators`
  - 参数：
    - `--db`
    - `--symbol`
    - `--start`
    - `--end`
    - `--recent-bars`
- `technical-analysis`
  - 参数：
    - `--db`
    - `--symbol`
    - `--start`
    - `--end`
    - `--recent-bars`
    - `--include-series`

说明：

- `technical-indicators` 偏向返回最新指标快照
- `technical-analysis` 偏向返回标准化完整技术分析结构

#### 修改项三：扩展 `etf_detail`

在现有返回结构中新增：

- `technical_analysis`

结构示例：

```json
{
  "kind": "etf_detail",
  "symbol": "512170",
  "technical_analysis": {
    "trade_date": "2026-03-11",
    "indicators": {
      "ma_150": 1.11,
      "rsi_14": 61.5
    }
  }
}
```

#### 修改项四：兼容现有动作

保持以下命令名不变：

- `signal-150ma`
- `batch-signal-150ma`
- `backtest-150ma`
- `batch-backtest-150ma`
- `trade-plan-150ma`

这样插件和现有 agent prompt 无需同步大改。

### 4.8 `stock-agent/trading-agent/README.md`

#### 目标

更新运行说明与新增命令。

#### 修改项

- 在“范围”中说明：
  - 技术分析能力已独立为 runtime 模块
  - 150MA 策略复用 MA 指标模块
- 在“快速开始”中新增命令示例：
  - `technical-indicators`
  - `technical-analysis`
- 在说明部分补充：
  - 技术分析与策略已解耦
  - 详情接口会返回技术指标摘要

### 4.9 `stock-agent/trading-agent/EXTENSIBILITY.md`

#### 目标

把“技术分析模块”明确为正式扩展点。

#### 修改项

- 新增一节“技术指标模块扩展约束”
- 说明：
  - 技术指标实现放 runtime
  - 策略只能消费技术指标结果，不应重复造轮子
  - 后续形态识别、趋势识别、波动识别统一接在 `technical_analysis.py`

### 4.10 `stock-agent/trading-agent/strategies/technical-analysis/README.md`

#### 目标

补一份面向业务与维护者的模块说明。

#### 新增内容

- 技术分析模块职责
- 已支持指标列表
- 指标列命名规范
- 后续扩展能力：
  - 形态识别
  - 趋势识别
  - 波动识别
  - 标准化特征

### 4.11 `stock-agent/trading-agent/strategies/strategy-150ma-v1/README.md`

#### 目标

更新策略说明，反映实现边界变化。

#### 修改项

- 明确写出：
  - `150MA` 策略本身不再负责计算 MA
  - `ma_150` 来自技术指标模块
  - 策略只负责：
    - 穿越判定
    - 回测交易路径
    - 交易计划生成

### 4.12 `stock-agent/trading-agent/strategies/strategy-150ma-v1/params.json`

#### 目标

将策略参数显式化。

#### 新增内容

至少包含：

```json
{
  "indicator_ref": {
    "ma_field": "ma_150"
  },
  "signal_rule": {
    "cross_up_action": "buy",
    "cross_down_action": "sell"
  }
}
```

#### 结果要求

- 后续若新增 `strategy-60ma-v1` 或组合策略，可复用同一指标模块

### 4.13 `extensions/trading-plugin/src/tool.ts`

#### 目标

扩展 OpenClaw 插件动作，让 agent 可显式调用技术分析能力。

#### 修改项一：动作枚举

在 `ACTIONS` 中新增：

- `technical_indicators`
- `technical_analysis`

#### 修改项二：工具参数

复用现有字段：

- `symbol`
- `start`
- `end`
- `recentBars`

新增可选字段：

- `includeSeries`

#### 修改项三：命令映射

将：

- `technical_indicators` 映射到 `technical-indicators`
- `technical_analysis` 映射到 `technical-analysis`

#### 修改项四：摘要函数

新增：

- `summarizeTechnicalIndicators(...)`
- `summarizeTechnicalAnalysis(...)`

摘要重点输出：

- 最新交易日
- `ma_150`
- 主要趋势均线
- `macd_hist`
- `rsi_14`
- `adx_14`

### 4.14 `extensions/trading-plugin/README.md`

#### 目标

补充新动作说明和示例调用。

#### 修改项

- 在“支持动作”中新增：
  - `technical_indicators`
  - `technical_analysis`
- 新增工具调用示例

### 4.15 `extensions/trading-plugin/src/tool.test.ts`

#### 目标

补充参数与命令映射测试。

#### 新增测试点

- `technical_indicators` 必须要求 `symbol`
- `technical_analysis` 必须要求 `symbol`
- `recentBars`、`includeSeries` 能正确透传

### 4.16 `extensions/trading-plugin/src/tool.summary.test.ts`

#### 目标

补充新 payload 摘要测试。

#### 新增测试点

- `technical_indicators` 的摘要输出
- `technical_analysis` 的摘要输出
- `etf_detail` 新增 `technical_analysis` 后摘要不回归

### 4.17 `stock-agent/trading-agent/tests/` 或同级测试文件

#### 目标

为 Python 技术指标与策略复用补测试。

#### 建议新增测试文件

- `stock-agent/trading-agent/scripts/test_indicators.py`
- `stock-agent/trading-agent/scripts/test_technical_analysis.py`
- `stock-agent/trading-agent/scripts/test_strategy_150ma.py`

#### 测试点

1. 指标模块
- MA/EMA 输出列正确
- MACD/RSI/KDJ/BOLL/ATR/OBV/ADX 均能返回预期列
- 数据不足时不崩溃

2. 技术分析聚合层
- 最新快照结构稳定
- `include_series` 时返回最近序列

3. 150MA 策略
- 信号判断仍正确
- 回测买卖点仍正确
- 交易计划仍引用同样的信号结果

4. 回归测试
- 重构后 `signal-150ma` 与历史逻辑在同一组样本上结果一致

## 5. 分阶段开发顺序

### 第一阶段：技术指标底座

涉及文件：

- `stock-agent/trading-agent/requirements.txt`
- `stock-agent/trading-agent/scripts/indicators_config.py`
- `stock-agent/trading-agent/scripts/indicators.py`
- `stock-agent/trading-agent/scripts/technical_analysis.py`

交付结果：

- 能对任意 symbol 的日线数据计算通用指标
- 能返回最新指标快照

### 第二阶段：150MA 策略改造

涉及文件：

- `stock-agent/trading-agent/scripts/strategies/__init__.py`
- `stock-agent/trading-agent/scripts/strategies/strategy_150ma.py`
- `stock-agent/trading-agent/scripts/stock_agent.py`
- `stock-agent/trading-agent/strategies/strategy-150ma-v1/README.md`
- `stock-agent/trading-agent/strategies/strategy-150ma-v1/params.json`

交付结果：

- `150MA` 策略完全复用 `ma_150`
- 原有 CLI 行为保持兼容

### 第三阶段：对外暴露技术分析能力

涉及文件：

- `stock-agent/trading-agent/README.md`
- `stock-agent/trading-agent/EXTENSIBILITY.md`
- `stock-agent/trading-agent/strategies/technical-analysis/README.md`
- `extensions/trading-plugin/src/tool.ts`
- `extensions/trading-plugin/README.md`

交付结果：

- OpenClaw agent 可直接请求技术分析数据
- `etf_detail` 可返回技术分析字段

### 第四阶段：测试与验收

涉及文件：

- `extensions/trading-plugin/src/tool.test.ts`
- `extensions/trading-plugin/src/tool.summary.test.ts`
- Python 相关测试文件

交付结果：

- 技术指标正确性有测试覆盖
- 插件调用链路有测试覆盖
- 150MA 重构无行为回归

## 6. 验收标准

完成后至少满足以下标准：

- `technical-indicators` 能返回指定 ETF 最新指标快照
- `technical-analysis` 能返回标准化技术分析结构
- `etf_detail` 能包含 `technical_analysis`
- `signal-150ma` 与 `backtest-150ma` 通过技术指标模块读取 `ma_150`
- `trade-plan-150ma` 行为不回归
- `extensions/trading-plugin` 能暴露新的技术分析动作
- WSL Ubuntu 环境可正常运行指标计算

## 7. 本轮实施边界说明

本方案已经按“技术分析能力与策略解耦”的方向收口，核心约束如下：

- 技术指标模块是独立能力层
- `150MA` 只是技术指标模块的一个消费者
- 后续形态识别、趋势识别、波动识别都继续挂在技术分析模块下扩展
- 策略实现不得重复在本地重新计算均线，应直接消费技术指标结果

这意味着后续如果要增加：

- `EMA 趋势策略`
- `MACD 趋势策略`
- `ATR 波动突破策略`

都不需要重新发明指标计算，只需增加策略层规则与测试。

## 8. 实际开发中遇到的问题与注意事项

- WSL Ubuntu 当前环境默认未安装 `TA-Lib`，开发与验证阶段需要允许技术指标模块回退到 pandas 算法，否则 `technical-indicators` 和 `technical-analysis` 无法直接跑通。
- `strategy_versions` 里的旧参数会随着已有 `industry.db` 一起保留；如果继续使用 `insert or ignore`，旧库不会拿到新的指标引用参数，因此初始化逻辑需要支持更新已有策略元数据。
- `150MA` 策略改造后，策略层只能读取 `ma_150`，不要在策略函数里再次写 `rolling(150)`，否则会重新耦合回旧结构。
- `etf_detail` 是当前 agent 最稳定的详情查询入口，因此新增技术分析字段时要优先保证它向后兼容，再扩展新的独立动作。
- OpenClaw 在 WSL 中会拒绝直接加载 `/mnt/f/...` 这种 world-writable 插件目录；实际服务验证时，需要把 `trading-plugin` 放到 `~/.openclaw/extensions/trading-plugin` 之类的受信路径。
- `stock-agent/trading-agent/references/openclaw.trading-agent.example.json` 里的旧字段可能与当前 OpenClaw 配置 schema 不兼容，启动前要先校验示例配置，避免因为过期键导致服务启动失败。
- agent 端到端测试和 CLI/插件直调测试要分开看：CLI 与插件动作可以已经通过，但 agent 是否稳定触发新动作，还取决于 WSL 中实际 workspace 的 `AGENTS.md` 和 skill 规则是否同步更新。
- 如果只是验证新技术指标能力，优先使用 `technical-indicators`、`technical-analysis` 和带 `technical_analysis` 字段的 `etf-detail` 做样例；如果要验证 agent 自然语言效果，还需要单独补 prompt/workflow。

## 8. 实际开发中遇到的问题与注意事项

- WSL Ubuntu 当前环境默认未安装 `TA-Lib`，开发与验证阶段需要允许技术指标模块回退到 pandas 算法，否则 `technical-indicators` 和 `technical-analysis` 无法直接跑通。
- `strategy_versions` 里的旧参数会随着已有 `industry.db` 一起保留；如果继续使用 `insert or ignore`，旧库不会拿到新的指标引用参数，因此初始化逻辑需要支持更新已有策略元数据。
- `150MA` 策略改造后，策略层只能读取 `ma_150`，不要在策略函数里再次写 `rolling(150)`，否则会重新耦合回旧结构。
- `etf_detail` 是当前 agent 最稳定的详情查询入口，因此新增技术分析字段时要优先保证它向后兼容，再扩展新的独立动作。
- OpenClaw 在 WSL 中会拒绝直接加载 `/mnt/f/...` 这种 world-writable 插件目录；实际服务验证时，需要把 `trading-plugin` 放到 `~/.openclaw/extensions/trading-plugin` 之类的受信路径。
- `stock-agent/trading-agent/references/openclaw.trading-agent.example.json` 里的旧字段可能与当前 OpenClaw 配置 schema 不兼容，启动前要先校验示例配置，避免因为过期键导致服务启动失败。
- agent 端到端测试和 CLI/插件直调测试要分开看：CLI 与插件动作可以已经通过，但 agent 是否稳定触发新动作，还取决于 WSL 中实际 workspace 的 `AGENTS.md` 和 skill 规则是否同步更新。
- 如果只是验证新技术指标能力，优先使用 `technical-indicators`、`technical-analysis` 和带 `technical_analysis` 字段的 `etf-detail` 做样例；如果要验证 agent 自然语言效果，还需要单独补 prompt/workflow。
