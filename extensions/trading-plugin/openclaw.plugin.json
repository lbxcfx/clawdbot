{
  "id": "trading-plugin",
  "name": "Trading Plugin",
  "description": "OpenClaw 的 trading-agent 运行时桥接工具。",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "pythonBin": {
        "type": "string",
        "description": "trading-agent 运行时使用的 Python 可执行文件。WSL/Linux 下建议使用 python3。"
      },
      "scriptPath": {
        "type": "string",
        "description": "指向 stock-agent/trading-agent/scripts/stock_agent.py 的路径。"
      },
      "dbPath": {
        "type": "string",
        "description": "SQLite 交易数据库路径。"
      },
      "bundledDbPath": {
        "type": "string",
        "description": "内置 SQLite 数据库路径。当 dbPath 不存在时，会先复制这份数据库。"
      },
      "seedCsvPath": {
        "type": "string",
        "description": "候选 ETF CSV 路径。当数据库里没有交易池成员时，会用它进行初始化导入。"
      }
    }
  }
}
