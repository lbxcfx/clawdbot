# strategies

这个目录用于保存 `trading-agent` 的正式策略资产。

建议每个策略单独一个目录，至少包含：

- `README.md`：策略说明
- `params.json`：关键参数

如果策略后续需要单独测试、样例数据或模板，也放在对应策略目录里。

示例：

```text
strategies/
  strategy-150ma-v1/
    README.md
    params.json
```
